* for using DML queries in UAT/ PROD, "db_datawriter" role is required.
* for using DDL queries in UAT/ PROD, "db_ddladmin" role is required.
* good to create a SQL FID (sql server auth) in UAT/ PROD and assign all roles to the SQL FID (App owned FID)
